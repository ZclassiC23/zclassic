# check_live_datadir_isolation — prong C baseline.
# Tracked docs/scripts that show an invocation of a leaf which
# ACCEPTS a datadir input, without naming one — so a reader who
# copies the line points it at the live node.
#
# Format: <path> <count>.  COUNTS MAY ONLY SHRINK.
# Fix a row by adding --datadir=/tmp/<fixture> to the example.
# Trailing per-row comments are hand analysis; regeneration keeps them.
# Regenerate: ZCL_LINT_MODE=UPDATE tools/lint/check_live_datadir_isolation.sh
docs/cookbook/12_sovereign_release.md 8   # zcode release/identity/epoch examples; a cookbook is copy-paste by design.
docs/RUNBOOK.md 2   # `core wallet restore` — operator doc, plausibly aimed at a real node ON PURPOSE. Decide before "fixing".
docs/SERVICES.md 1   # `app service access <name>` — THE command that ran the boot ceremony on the live node.db. Highest priority row.
docs/spec/sovereign-identity-layer.md 1   # `zcode proof walk`, described as read-only in prose.
docs/WALLET_PERSISTENCE_RECOVERY.md 1   # `core wallet restore` — same class as RUNBOOK.md.
