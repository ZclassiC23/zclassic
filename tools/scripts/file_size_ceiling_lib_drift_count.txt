# Lint gate E1 WARN-tier drift-count ratchet.
#
# tools/scripts/check_file_size_ceiling.sh's WARN tier (lib/+domain/+src/)
# never fails on any ONE oversized/grown file — but with nothing ratcheting
# the total violation count, drift accumulated silently: 5 new unbaselined
# files + 13 files grown past their recorded baseline LOC, none of it ever
# failing `make lint`.
#
# This file records the highest (new + grown) violation COUNT reviewed and
# accepted so far. The gate reads the first non-comment, non-blank line as
# an integer and FAILS when the live count exceeds it — existing drift (at
# or below this count) stays grandfathered and WARN-only, but growing PAST
# this count fails the build until either a file gets re-baselined/shrunk
# (lowering the live count back at or below this line) or this count is
# deliberately raised after review.
#
# Seeded 2026-07-19 at the count measured after baselining the 5 new files
# (rom_fetch.c, rom_seed.c, census_read.c, peers_projection.c, stage.c) and
# src/main.c: the remaining 13 already-baselined files that had grown past
# their recorded LOC (command_registry.c, addrman.c, connman.c, download.c,
# file_service.c, msg_headers.c, net.c, onion_service.c, peer_lifecycle.c,
# simnet_chaos_faults.c, coins_kv.c, supervisor.c, chainstate.c).
#
# Reconciled 2026-07-19 (later same day) to 15: the 6-file baseline addition
# above (5 new files + src/main.c) had been silently dropped by an
# intervening merge — check_file_size_ceiling.sh was re-reporting all 6 as
# "NEW oversized file" on main, which is restored regression, not fresh
# drift. Restoring those 6 baseline entries plus the 13 already-grandfathered
# grown files brings new+grown back to 0+13=13 — except two more files grew
# past their recorded LOC in the interim (real drift since this ratchet was
# first seeded): fast_sync.c (2032 -> 2131) and msgprocessor_snapshot.c
# (2452 -> 2464). Reviewed and accepted; ratchet raised from 13 to 15 to
# cover them. Both stay WARN-only per-file (lib/net/, feature growth, not
# baselined at their new LOC — the ratchet grandfathers the count, not the
# individual entries).
#
# Raised 14 -> 15 (task J2, boot-time integrity failsafes): consensus_db.c
# grew 846 -> 883 for the binary-downgrade schema-marker refusal
# (consensus_db_schema_is_downgrade / consensus_db_finalize_flip). Reviewed
# and accepted; re-baselined in file_size_ceiling_lib_baseline.txt.
#
# Paydown 2026-07-19 (later still) to 14: msgprocessor_snapshot.c (2676
# lines, the fastest-drifting file, +224 over its 2452 baseline) split by
# responsibility — the SERVE side (cached offer/manifest/block-manifest
# publish+accessor APIs, the zchunkreq/zblkreq client-puzzle PoW guard,
# send_snapshot_offer_msg/push_manifest/push_block_manifest/
# build_block_piece_payloads, and the per-command serve handlers the
# mp_handle_zcl23_sync dispatcher calls into) moved to the new
# lib/net/src/msgprocessor_snapshot_serve.c, mirroring the connman.c/
# connman_dialer.c split shape. msgprocessor_snapshot.c is back under its
# (retightened) baseline of 1666, removing it from the grown-past-baseline
# list; msgprocessor_snapshot_serve.c is freshly baselined at its own
# starting size (1108) so future growth on IT becomes visible the same way.
# Net: one real paydown against the J2 raise, 15 -> 14.
#
# 2026-07-19 (K3 throughput levers): src/main.c grew past its recorded 4612
# baseline (+6 lines) to register two new default-OFF CLI flags,
# -prefetch-blocks and -derive-drain-batch. main.c is the drift-tier monolith
# already tracked here; this is one reviewed +6-line flag registration, not a
# new oversized file. Ratchet raised 14 -> 15 to cover it (stays WARN-only
# per-file; the baseline entry is NOT raised).
#
# Paydown 2026-07-19 (lane Q4, ratchet re-tighten): src/main.c dropped out of
# the WARN tier entirely via the p1-main-decompose split (4617 -> 515 lines,
# pure code-motion into config/src/args.c + src/main_cli_modes.c), removing
# its grown-past-baseline entry from the live count with nothing to
# re-baseline (515 is well under the 800 ceiling). Live drift measured at
# 13 (all pre-existing grown-file WARNs, no new unbaselined files). Ratchet
# lowered 15 -> 13.
#
# 2026-07-20 (lane wf/a4-rom-hardening): rom_fetch.c grew 1420 -> 1427 to
# wire net/rom_peer_scoring.h's rom_peer_note_bad_chunk() into the two
# existing digest/MAC-mismatch sites (transport MAC check in
# rom_fetch_chunk, per-chunk content verify in the resume worker) — a
# 2-call, 1-include wiring change, not new growth to pay down. rom_seed.c
# grew 891 -> 892: rom_seed_dump_state_json() was missing json_set_object(out)
# before its json_push_kv_* calls (every field silently no-op'd on a
# non-JSON_OBJ target — json_push_kv() returns false when obj->type !=
# JSON_OBJ), a pre-existing bug this lane's byte-accounting test exposed as
# its first real caller; one-line fix matching every other *_dump_state_json
# in the codebase. Both re-baselined in file_size_ceiling_lib_baseline.txt
# (1427, 892); count stays at 13.
#
# Paydown 2026-07-20 (lane G3, sync fault matrix): simnet_chaos_faults.c grew
# 1092 -> 2038 lines adding six new named fault injectors (g)-(l) over the
# ROM-artifact delivery path (rom_fetch.c/rom_journal.c/rom_seed.c) and the
# pure sync_reduce kernel, mirroring the file's existing (a)-(f) shape — real
# growth, not incidental. Re-baselined in file_size_ceiling_lib_baseline.txt
# (2038), which removes its grown-past-baseline entry from the live count
# (nothing else changed). Live drift measured at 12. Ratchet lowered 13 -> 12.
#
# 2026-07-21 (wf/snapshot-silent-accept, D1): msgprocessor_snapshot.c grew
# 1705 -> 1746 adding msg_swarm_utxo_sha3_verify() — the swarm chunk-sync
# aggregate SHA3 mismatch used to fall through into the same cleanup as a
# PASSED verify with no peer offence and no blocker (invisible-fault seam);
# the new function scores PEER_OFFENCE_INVALID_PROOF and names a typed
# blocker on mismatch. Re-baselined in file_size_ceiling_lib_baseline.txt
# (1746); count unchanged.
#
# 2026-07-21 (lane wf/disruption-resume, G4): simnet_chaos_faults.c grew
# 2038 -> 2290 lines adding fault (m), chaos_fault_peer_disconnect_mid_body_
# download() — the P2P block-body download/resume disruption fault (the
# real download_manager + syncsvc_collect_needed_blocks(), distinct from
# the (g)-(l) rom_journal/artifact matrix already in this file). Real
# growth, mirroring the file's own established (a)-(l) shape. Re-baselined
# in file_size_ceiling_lib_baseline.txt (2290); count unchanged.
#
# 2026-07-21 (wf/net-body-flow, N2): msgprocessor.c grew 2344 -> 2373 adding
# Rule C (body-download stall disconnect) — a peer that serves headers but
# returns zero block bodies passes every header-stall check yet holds an
# outbound slot forever while the body cursor never advances (the measured
# "connected peer delivers no block data" wedge); the new call site fails
# over off it by disconnecting so connman dials a peer that will serve
# bodies. Re-baselined in file_size_ceiling_lib_baseline.txt (2373); count
# unchanged.
#
# 2026-07-21 (wf/body-stall-dark): msgprocessor.c grew 2373 -> 2415 adding
# Rule D (delivered-then-dark body-download stall disconnect) — the complement
# of Rule C. Rule C exempts any peer with body_received > 0, so a peer that
# serves a few bodies then goes silent holds its outbound slot forever while
# the body cursor never advances again; DL_STALL_TIMEOUT_SECS sat
# defined-but-unenforced. The new call site evicts such a peer once its last
# body is >= the stall window old AND it has accrued the same timed-out floor
# Rule C uses. Re-baselined in file_size_ceiling_lib_baseline.txt (2415);
# count unchanged (download.c stays grandfathered, msgprocessor.c stays at
# baseline).
#
# 2026-07-21 (wf/rls-auto-discovery, instant-on peer discovery): file_service.c
# grew 1877 -> 2187 (the "RLS" ROM directory-listing serve opcode + handler,
# sibling of the existing "RMF" per-chunk-manifest serve) and rom_fetch.c grew
# 1520 -> 1622 (rom_fetch_get_directory, the clearnet client half — connect,
# send RLS, MAC-verify the {"artifacts":[...]} body). Both are real,
# shape-matching feature growth wiring peer directory discovery so a fresh
# empty-datadir node finds the checkpoint bundle manifest with zero operator
# input. Re-baselined in file_size_ceiling_lib_baseline.txt (2187, 1622); count
# unchanged.
#
# 2026-07-21 (wf/headers-artifact, Lane A3 headers-as-artifact fast path):
# rom_seed.c grew 892 -> 904 adding rom_seed_kind_from_name() (the inverse of
# the directory.json "kind" wire token, so the boot picker selects the header
# seed vs the consensus bundle by kind rather than size); rom_fetch.c grew
# 1622 -> 1626 parsing that "kind" field in rom_fetch_parse_directory. Both are
# small, shape-matching wiring for the swarm-downloaded block_index.bin header
# seed. Re-baselined in file_size_ceiling_lib_baseline.txt (904, 1626); count
# unchanged. (NOTE: integration/instant-on-3fleet later merged wf/reseed-widening
# which also grows rom_seed.c — the merge reconciles the two baseline bumps.)
#
# 2026-07-23 (wf/rom-chunk-serve-fix, ROM chunk-serve robustness): file_service.c
# grew 2187 -> 2234 adding fs_send_chunk_refusal() — a typed ROM chunk refusal
# frame ([4B sentinel][1B reason][32B MAC]) that replaces the encrypted FS_DONE
# frame the raw chunk reader misparsed as a garbage "implausible chunk size";
# rom_fetch.c grew 1626 -> 1703 adding the client-side decode (rom_fetch_wire_is_
# refusal / rom_fetch_refusal_reason_name + the sentinel-decode branch in
# rom_fetch_chunk) so a busy seeder's refusal reads as a clean back-off + retry
# instead of aborting the whole parallel download. Both are real, shape-matching
# feature growth on the starter-pack delivery path. Re-baselined in
# file_size_ceiling_lib_baseline.txt (2234, 1703); count unchanged.
#
# 2026-07-24 (wf/package-factory-gaps, fresh-bundle package factory): rom_seed.c
# grew 977 -> 1043 adding rom_seed_deregister() (the register inverse the
# exporter's rotation calls before unlink so a deleted generation stops being
# served) + rom_bundle_height_from_name() and the directory.json "height" field
# (GAP-4 newest-by-height selection). rom_fetch.c grew 1703 -> 1709 parsing that
# optional "height" onto rom_fetch_manifest.height (download-cosmetic, untrusted).
# Both are real, shape-matching feature growth on the starter-pack delivery path.
# Re-baselined in file_size_ceiling_lib_baseline.txt (1043, 1709); count unchanged.
#
# 2026-07-24 (wf Program-O4e, wallet cached-stmt reset audit): wallet_sqlite.c
# grew 1383 -> 1425 fixing the cached-SELECT snapshot-pin class — read_single_key
# now copies the privkey blob out and resets the cursor UNCONDITIONALLY (it
# parked on SQLITE_ROW after every successful lookup), and read_keys / read_txs /
# read_sapling_keys (whose loop BREAKs at MAX_SAPLING_KEYS) / read_scripts /
# read_watch_only reset after their drain loop so a mid-iteration error or break
# can no longer leave a cursor pinning the WAL snapshot (SQLITE_BUSY_SNAPSHOT for
# every later writer on the shared handle). Real defensive growth. Re-baselined
# in file_size_ceiling_lib_baseline.txt (1425); count unchanged.
#
# 2026-07-24 (resilience proof harnesses): simnet_chaos_faults.c grew
# 2290 -> 2684 adding three named fault injectors (n)-(p) —
# chaos_fault_torn_progress_wal (a truncated un-checkpointed WAL),
# chaos_fault_disk_full_pause (SQLITE_FULL on a coins write), and
# chaos_fault_cleared_have_data_hole (a vanished persisted body) — mirroring
# the file's established (a)-(m) shape. Real growth, same class as the G3 and
# G4 injector additions above. Re-baselined in
# file_size_ceiling_lib_baseline.txt (2684); count unchanged.
#
# 2026-07-25 (module-graph acyclicity, metrics -> rpc cut): prometheus_metrics.c
# grew 1084 -> 1100 replacing its direct rpc_http_middleware_get_global() /
# rpc_http_middleware_stats_snapshot() calls with a registered counter source
# (metrics_prometheus_set_rpc_http_source), so lib/metrics no longer links
# against lib/rpc. Re-baselined in file_size_ceiling_lib_baseline.txt (1100),
# which keeps it OUT of the drift list rather than spending the last slot under
# the ratchet; live count 12. Ratchet left at 13.
#
# 2026-07-25 (Groth16 spend-circuit port, sections 8-9): circuit_gadgets.c grew
# 1574 -> 1646 NET — it gained gadget_into_bits_le_strict + gadget_point_repr
# (bellman's strict in-field bit decomposition and EdwardsPoint::repr, 388
# constraints per field element) and simultaneously SHED 103 lines of dead
# unsound stubs (gadget_blake2s, gadget_merkle_path, gadget_note_commitment,
# gadget_nullifier — all four computed their result out of circuit and
# allocated the answer with no constraint binding output to input; none had a
# caller). groth16_prover.c grew 1036 -> 1057 adding cs_is_satisfied(), the
# A*B==C check over the witness that the parity oracle needs to see a wrong
# constraint COEFFICIENT (counts and probed wire values cannot). Both
# re-baselined here rather than spending a ratchet slot; count unchanged.
#
# 2026-07-26 (fold perf, incremental-tree root memoization):
# incremental_merkle_tree.c grew 984 -> 1019 adding the runtime-only
# cached-root fields + per-mutator invalidation (root memo in the tree
# struct so repeated incremental_tree_root() calls on an unmutated tree
# skip the full Pedersen fold; bit-identical output, wire format
# unchanged). Re-baselined in file_size_ceiling_lib_baseline.txt (1019);
# count unchanged.
#
# 2026-07-26 (swarm throughput: pipeline depth + block-piece rate split):
# fast_sync.c grew 1889 -> 1899 (fast_sync_rate_check refactored into the
# parameterized fast_sync_rate_check_n + the original 2-arg wrapper) and
# msgprocessor_snapshot_serve.c grew 1108 -> 1114 (dedicated
# g_blk_rate_limiter instance + call-site switch + its comment). Block
# pieces now draw from FAST_SYNC_MAX_BLOCK_PIECES_PER_HOUR (100k/hr per
# IP) instead of the snapshot-chunk bucket (5k/hr) that starved a
# single-peer swarm sync mid-download. Both re-baselined in
# file_size_ceiling_lib_baseline.txt; count unchanged.
#
# 2026-07-27 (wf wallet-secret single-writer, plaintext-mirror elimination):
# wallet_sqlite.c grew 1425 -> 1648 adding wallet_sqlite_scrub_plaintext_r()
# (the boot STATE-G one-shot in-place WKS1-envelope upgrade of any remaining
# plaintext wallet_keys/wallet_sapling_keys/wallet_seed rows) plus the
# wsql_emit_transparent_key_add / wsql_emit_sapling_key_add event+projection
# feed moved here from the deleted app/models wallet_key save hooks (the
# model-layer save path that wrote the plaintext mirror is gone; the primary
# writer is now the only writer). Real defensive growth. Re-baselined in
# file_size_ceiling_lib_baseline.txt (1648), removing it from the drift list;
# live count back to 13. Ratchet left at 13. Same task: config/src/boot_services.c
# shed the deleted boot_wallet()/node_db_sync_wallet_keys() mirror call (-12)
# and regained exactly those 12 lines for the STATE-G scrub wiring (boot.c had
# zero baseline headroom) — size unchanged at its 1477 baseline.
#
# 2026-07-27 (node.db lock-contention fixes, work/db-lock-fixes): fast_sync.c
# grew 1899 -> 1909 — fast_sync_apply_chunk's private READWRITE apply
# connection gains a 30 s busy timeout (matching the read-only sibling) and
# a BEGIN IMMEDIATE (the deferred BEGIN's first INSERT could fail instantly
# with SQLITE_BUSY_SNAPSHOT against a concurrent catchup/coins commit).
# Re-baselined in file_size_ceiling_lib_baseline.txt (1909); count unchanged.
#
# 2026-07-27 (ZCODE slice 6, external verifier attestations):
# os_sandbox_linux.c grew 1396 -> 1415 — os_sandbox_path_rule gains
# allow_execute/allow_create (mapped to Landlock EXECUTE and the
# MAKE_REG/MAKE_DIR/REMOVE_FILE/REMOVE_DIR family, dir-bits masked off for
# file rules) so zclassic23-package-verify's build children can exec the
# toolchain and create objects inside their confined build dir. Re-baselined
# in file_size_ceiling_lib_baseline.txt (1415); count unchanged.
#
# 2026-07-27 (ZCODE slice 7, contribution scoring): package_score.c is a
# NEW 845-line file (the semantic-line classifier + logical-statement
# unitizer + lineage content diff + category/cap arithmetic — one
# cohesive scoring module mirroring package_store.c's one-file shape).
# Freshly baselined at its starting size (845) so future growth on it
# becomes visible; count unchanged.
13
#
# 2026-07-27 (os_sandbox dynamic Landlock handled-set): os_sandbox_linux.c
# grew 1415 -> 1422 — the slice-6 unconditional handled-set addition of
# EXECUTE/MAKE_REG/MAKE_DIR/REMOVE_FILE/REMOVE_DIR denied those rights to
# every pre-existing caller (node -confine profiles never set the new
# flags; test_confine's confined file I/O + serving loops failed). The
# handled set is now computed from the rules: new bits only when some rule
# sets allow_execute/allow_create. Re-baselined in
# file_size_ceiling_lib_baseline.txt (1422); count unchanged.
#
# 2026-07-27 (ZCODE slice 8, simulated reward settlement): package_reward.c
# is a NEW 2112-line file (the reward-history ledger + daily settlement
# queue: canonical wires, durable tmp+fsync+rename IO, load/replay, enqueue
# validation, batch-atomic cap-aware planning, idempotent commit, and
# receipts — one cohesive settlement module mirroring package_store.c's
# one-file shape). Freshly baselined at its starting size (2112) so future
# growth on it becomes visible; count unchanged.
#
# 2026-07-27 (ZCODE slice 9, rankings): package_reward.c grew 2112 -> 2135
# adding vcs_reward_ledger_fact_at() — the read-only settled-fact accessor
# the rankings projection (lib/vcs/package_rank.c, a new 557-line module,
# under the 800 ceiling) reads the ledger through. Re-baselined in
# file_size_ceiling_lib_baseline.txt (2135); count unchanged.
#
# 2026-07-27 (ZCODE slice 10, simulated badges): package_badge.c is a NEW
# 1339-line file (the badge wire codec + id + secp256k1 verify + policy lens
# + durable badge/plan/commit store — one cohesive badge module mirroring
# package_store.c's one-file shape) and package_badge_eligible.c is a NEW
# 816-line file (the pure 13-type evaluator + facts builder + plan build +
# two-pass issue). Both freshly baselined at their starting sizes (1339,
# 816) so future growth on them becomes visible; count unchanged.
# Same lint run exposed one restored regression already committed on this
# branch: codeindex_merkle.c (1070 lines, be8471cf1) had never been
# baselined. Freshly baselined at 1070; count unchanged.
#
# 2026-07-27 (ZCODE slice 12, authenticated package swarm):
# package_swarm_node.c is a NEW 1800-line file (the swarm engine: peer
# table + per-peer offence accounting, manifest-first download scheduler
# with named terminal stalls, per-chunk attempt caps, tick-driven outbound
# queue, and the frame handler returning typed penalty/rule/disconnect —
# one cohesive engine module mirroring package_store.c's one-file shape).
# Freshly baselined at its starting size (1800) so future growth on it
# becomes visible; count unchanged. Same slice: package_store.c grew
# 964 -> 1069 adding the swarm read APIs (get_chunk_at, get_manifest_wire,
# chunk_present, list_summaries) — re-baselined at 1069; count unchanged.
# msgprocessor.c grew 2415 -> 2472 (the zpkgswm dispatch row, the tick
# hook call, and the hook-field NULLs; 2462 of it predates this slice on
# this branch) — re-baselined at 2472, which also absorbs that restored
# branch drift; count unchanged.
#
# 2026-07-27 (ZCODE slice 13, onion/HTTPS site): https_server.c grew
# 951 -> 982 and onion_service.c grew 1021 -> 1031 wiring the /zcode*
# prefix into both dispatch chains (one bounded handler call each, the
# site controller living under app/controllers/). Both re-baselined in
# file_size_ceiling_lib_baseline.txt (982, 1031); count unchanged.
# Same lint run exposed one restored regression already committed on
# this branch: package_service.c (1066 lines, 9f72e1d33) had never been
# baselined. Freshly baselined at 1066; count unchanged (live 11).
#
# 2026-07-28 (onion directory: names in discovery + row freshness):
# onion_service.c grew 1031 -> 1229 serving the ZNAM name beside every raw
# .onion on /search, /directory and /directory.json, and filtering + dating
# rows by age. The freshness LEDGER itself (the peer_directory table + its
# freshness columns, the expiry sweep, the v3 hostname predicate, the
# /directory.json onion-field parser, and the read-only ZNAM join) did NOT
# land in that file: it is the new lib/net/src/onion_directory.c.
# onion_service.c re-baselined in file_size_ceiling_lib_baseline.txt (1229);
# count unchanged. connman.c gained ~60 lines walking the onion half of a
# peer directory response and stays grandfathered at its existing WARN entry.
#
# 2026-07-28 (identity/directory wave, merge reconciliation):
# Three entries BASELINED rather than raising the ceiling, so the recorded
# count stays 13 and the net move is a tightening:
#   lib/crypto/src/ed25519.c   993 — batch verification with the per-signature
#       torsion screen (the cofactorless verifier makes the textbook batch
#       construction unsound; the screen is the correctness fix, not padding).
#   lib/util/src/blocker.c     836 — the named blockers the directory-safety
#       work added; a blocker that cannot be named is the silent halt this
#       project forbids.
#   lib/net/src/onion_service.c 1229 -> 1235 — the six lines are the merge of
#       two lanes' independent edits to the same discovery function, not new
#       feature growth. The freshness ledger itself landed in the new
#       under-ceiling lib/net/src/onion_directory.c.
#
# 2026-07-28 (t4.3 directory freshness + onion graph, merged into the wave
# above): two teams had independently built lib/net/src/onion_directory.c at
# the same path. The merged file is ONE directory: the lifecycle half (the
# table, the freshness rules, the supervised refresh round, the relay-hint
# parser and follow budget, the census bridge) plus the name half (the
# renderable-label guard, the /directory.json onion-field scanner, and the
# read-only ZNAM name join). It also absorbed the second copy of the v3
# hostname predicate — onion_hostname_is_valid_v3 is deleted and every call
# site now holds the ONE onion_hostname_valid in onion_peer_merge.c, so the
# two can no longer drift. Union size 865, past the 800 ceiling, so it is
# BASELINED at 865 rather than left unlisted to trip the new-file WARN; that
# keeps future growth on it visible without spending a ratchet slot.
# onion_service.c settles at 1185 (BELOW main's 1235 entry — the merge moved
# the directory writers out and kept only the front door and the two
# serve_directory_* renderers), so its baseline is TIGHTENED 1235 -> 1185.
# connman.c 2881 -> 2951 for the merged onion half of try_onion_seed_fetch
# (scan the "onion" field, record every hint, follow a bounded few one hop —
# and, per the cold-boot fix in the same change, follow only when we are
# already above the healthy peer floor) — re-baselined at 2951.
# Every entry here is a baseline, not a raise: the recorded count STAYS 13.
