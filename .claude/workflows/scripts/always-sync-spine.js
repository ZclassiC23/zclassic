export const meta = {
  name: 'always-sync-spine',
  description: 'Always-sync-to-tip spine: fix the active-chain-tip linchpin, add an acting liveness authority, supervise the supervisor, build the chaos harness',
  phases: [ { title: 'Spine', detail: '4 isolated worktree lanes: Pillar 0 linchpin, Pillar 1 acting authority, Pillar 7 supervise-the-supervisor, chaos+G-TIP harness' } ],
}
const LANE_SCHEMA = {
  type: 'object', additionalProperties: false,
  required: ['branch','verdict','files_changed','summary','gates'],
  properties: {
    branch:{type:'string'}, verdict:{type:'string', enum:['MERGE_READY','NEEDS_WORK','BLOCKED']},
    files_changed:{type:'array', items:{type:'string'}}, summary:{type:'string'}, gates:{type:'string'}, caveats:{type:'string'},
  },
}
const COMMON = `zclassic23 is one C23 binary, a full ZClassic node, consensus-parity with zclassicd. MISSION: the node must ALWAYS fold to the network tip and never silently stall; a stall is always a NAMED, SELF-HEALED blocker at a known height, never an operator page for a recoverable cause. DOCTRINE: self-derived folding of real block bodies is the ONLY authority for the tip; borrowed/assisted state is scaffolding that must be re-derived and discarded.

MODEL (docs/HOW_THE_NODE_WORKS.md): a durable append-only log (progress.kv) folded by 8 reducer stages (each advance-cursor-or-name-a-typed-blocker); health = network_tip - H* (log_head = tip_finalize cursor); the supervisor liveness tree (lib/util/src/supervisor.c, 1s sweep) DRIVES the reducer drain every tick (app/supervisors/src/staged_sync_supervisor.c:398). Detection of a stall is comprehensive; the gap this workflow closes is ACTION and the active-chain-tip linchpin.

HARD RULES: consensus parity inviolable (no tx/block validity, Equihash, activation-height change; validate against the real chain not zclassicd text); every malloc via zcl_malloc, every error return LOG_*, every write AR lifecycle; files < 800 lines (app/+config ENFORCED); every thread supervised (Gate #23); NEVER touch a live datadir (~/.zclassic-c23), the mint producer (~/.zclassic-c23-mint), or stop zclassicd. Copy-prove before any live effect; gate on H* CLIMB, not "booted without FATAL." If your worktree cannot link, FIRST: cp -a /home/rhett/github/zclassic23/vendor/lib vendor/. You are in your OWN isolated git worktree — commit to THIS branch only; do NOT push/merge/touch others. Self-gate: make build-only + focused test group (make t-fast ONLY=<group>) + make lint (+ agent_impact_rules.def per changed .c; reconcile DOC-COUNTS / EXPECTED_DIAGNOSTICS_DUMPERS if a dumper). Final message = structured data, technical; report the branch name.`

phase('Spine')
const lanes = await parallel([
  () => agent(`${COMMON}

LANE Pillar 0 — make the tip REAL from the header index (THE LINCHPIN; consensus-adjacent). ROOT CAUSE (verified): active_chain_tip() (lib/validation/src/chainstate.c:278-297) returns NULL on a full-index boot because load_block_index() (app/services/src/block_index_loader.c:746-844) fills the header MAP + block_index_forward_pass (:840) but NEVER populates the active_chain[] window or pindex_best_header (only the empty-datadir fallback sets genesis, :810-812). tip_finalize_stage_init reads the empty raw slot (app/jobs/src/tip_finalize_stage.c:646 active_chain_cached_tip) and never re-anchors. Then push_getheaders -> active_chain_tip()==NULL -> genesis-only locator (app/services/src/header_sync_service.c:521-528), and the periodic-kick NULL-guard DISCARDS pindex_best_header (lib/net/src/msg_headers.c:1133 the 'tip &&', also :897-902 and :1108-1117) falling to genesis. Symptom: header sync pins at ~160/192960; NOTHING escalates it.
FIX (two changes, belt-and-suspenders, do NOT change consensus validity — only which header the chain/locator anchors at): (1) After load_block_index builds the map+forward pass, promote the best header (max-chainwork tip, or the coins-best/finalized height when higher) into the active_chain[] window and set pindex_best_header, REUSING the existing chain_set_active_tip(bi, TIP_FROM_P2P_REPAIR) seam (app/services/src/block_index_integrity.c:729 / chain_restore_*). So active_chain_tip() is NEVER NULL when the map is non-empty. Make it idempotent + safe when coins-reconcile later re-anchors. (2) In lib/net/src/msg_headers.c and app/services/src/header_sync_service.c, when active_chain_tip() is NULL, anchor the getheaders locator at pindex_best_header (or the highest map header) — NEVER genesis. Add a focused test (a datadir with a full block index but empty active-chain window: active_chain_tip() non-NULL after load; locator built from best_header not genesis). Files: block_index_loader.c, chainstate.c (only if a new accessor is needed), msg_headers.c, header_sync_service.c, tip_finalize_stage.c (only if init must re-anchor). This is the highest-priority lane; report exactly what you changed so the orchestrator can copy-prove H* CLIMB on the sovereign fold-forward.`,
    { isolation:'worktree', model:'opus', label:'P0:active-chain-tip', phase:'Spine', schema:LANE_SCHEMA }),

  () => agent(`${COMMON}

LANE Pillar 1 — one ACTING liveness authority (close detect->act). Today the two most direct "fold not advancing" signals are REPORT-ONLY: sync_rate_below_floor (app/conditions/src/sync_rate_below_floor.c — "ONLY names a slowdown, never acts on the fold", ~:317) and reducer_drive_watchdog (app/conditions/src/reducer_drive_watchdog.c — "no automated repair seam", ~:157). And the stage watchdogs create blockers with action=name_blocker only. FRAMEWORK.md §5's "one tip_not_advancing Condition is the sole liveness authority" is aspirational, not implemented.
IMPLEMENT: make the growing-gap signal ACT. On a sustained network_tip - H* gap (reuse sync_rate_below_floor's existing detector: network_tip=connman_max_peer_height, log_head=tip_finalize cursor), have its remedy INVOKE the recovery ladder — recovery_coordinator_run() (app/services/src/recovery_coordinator.c) and/or sticky_escalator_note_stall() (app/services/src/sticky_escalator.c:733) — instead of returning COND_REMEDY_FAILED. AND register corrective escape actions (blocker_register_escape, lib/util/src/blocker.c:421) on the stage/drive-stall blockers (stage_stalled_*, reducer_drive_stuck) so the blocker_supervisor_sweep ACTUATES a remedy at the deadline (re-kick the drive / invoke the ladder) rather than only surfacing. Keep the operator page as the LAST resort after the self-derived ladder is exhausted (never for a recoverable cause). Focused test: a synthetic growing-gap / frozen-cursor state triggers a ladder invocation (witnessed), not just a named blocker. Files: sync_rate_below_floor.c, reducer_drive_watchdog.c, and the escape-registration site (do NOT edit chainstate.c/block_index_loader.c/msg_headers.c — Pillar 0 owns those; do NOT edit supervisor.c — Pillar 7 owns it).`,
    { isolation:'worktree', model:'opus', label:'P1:acting-authority', phase:'Spine', schema:LANE_SCHEMA }),

  () => agent(`${COMMON}

LANE Pillar 7 — supervise the supervisor (close the last SPOF). The root zcl_supervisor thread (lib/util/src/supervisor.c:475-515, 1s sweep) cannot watch itself and is excluded from Gate #23 (tools/lint/check_thread_supervision.sh:56-60); its only backstop is the shutdown-path alarm() (:538-540). If it wedges, the drive it powers stops silently.
IMPLEMENT a minimal independent backstop: expose a monotonically-incrementing sweep counter/heartbeat from the supervisor sweep (atomic, already ticking every ~1s), and add a tiny independent watcher that force-restarts the process (re-exec /proc/self/exe, mirroring chain_tip_watchdog's off-systemd self-respawn at src/main.c:3934-3955) if that counter freezes for N seconds. PREFER wiring the systemd WATCHDOG=1 path (config/src/boot_sd_watchdog.c already exists) to be fed FROM the supervisor sweep heartbeat (so a frozen supervisor stops the systemd ping and systemd restarts), AND add the tiny off-systemd watcher as the fallback. The watcher does nothing but watch the watcher — keep it dead-simple and itself unkillable-by-the-thing-it-watches (separate thread with its own clock, or the alarm() timer). Add a focused test: a stalled/frozen sweep counter triggers the restart decision (test the decision function, not an actual re-exec). Files: supervisor.c (expose the heartbeat), a new small watcher (lib/util or config/src), boot_sd_watchdog.c, boot wiring in config/src/boot_services.c. Do NOT edit sync_rate_below_floor.c / reducer_drive_watchdog.c (Pillar 1 owns those).`,
    { isolation:'worktree', model:'sonnet', label:'P7:supervise-supervisor', phase:'Spine', schema:LANE_SCHEMA }),

  () => agent(`${COMMON}

LANE chaos + G-TIP harness (the always-sync PROOF infrastructure — test/sim only, no production code). Build a fault-injection harness that proves self-recovery, extending the deterministic simnet (lib/sim/include/sim/simnet.h) + postmortem. Provide reusable fault injectors a test can call on a simnet/fixture node: (a) EMPTY the active-chain window while the block-index map is full (reproduces the Pillar-0 linchpin wedge); (b) KILL/restart mid-fold; (c) CORRUPT a sealed chain_segment; (d) FREEZE the reducer drive; (e) STALL a single stage. Then a harness assertion: after each fault, the node returns to SERVING and H* resumes climbing to the target WITH ZERO operator action and NO EV_OPERATOR_NEEDED for a recoverable cause (poll dumpstate reducer_frontier hstar + the blocker registry + the operator-needed event count). Deliver the G-TIP test now (full block index + empty active-chain window -> after boot, active_chain_tip non-NULL / locator anchored at best_header / H* climbs) as a table-driven case even if Pillar 0's production fix lands in a sibling lane — wire it to SKIP-or-assert based on whether the fix is present, so it becomes the regression gate. Files: lib/sim/* + lib/test/src/test_always_sync_chaos.c (+ registration in test_parallel.c + agent_impact_rules.def + DOC-COUNTS). No production .c changes.`,
    { isolation:'worktree', model:'sonnet', label:'harness:chaos-gtip', phase:'Spine', schema:LANE_SCHEMA }),
])
return { lanes: lanes.filter(Boolean) }
