export const meta = {
  name: 'always-sync-self-healing',
  description: 'Pillars 2+4: the universal self-derived re-derive primitive + a complete never-give-up ladder + LCC write-rules that make a wedge unwritable',
  phases: [ { title: 'SelfHeal', detail: '4 isolated worktree lanes: stage_rederive_range primitive, complete/collapse escalator rungs, LCC write-rules, self-healing tests' } ],
}
const LANE_SCHEMA = {
  type: 'object', additionalProperties: false,
  required: ['branch','verdict','files_changed','summary','gates'],
  properties: { branch:{type:'string'}, verdict:{type:'string', enum:['MERGE_READY','NEEDS_WORK','BLOCKED']}, files_changed:{type:'array',items:{type:'string'}}, summary:{type:'string'}, gates:{type:'string'}, caveats:{type:'string'} },
}
const COMMON = `zclassic23 is one C23 node, consensus-parity with zclassicd. MISSION: always fold to tip; a stall is always a NAMED, SELF-HEALED blocker, never an operator page for a recoverable cause; self-derived folding of real bodies is the ONLY authority. This workflow builds Pillars 2 (a complete self-derived recovery ladder) and 4 (LCC write-rules so a wedge is unwritable), per docs/work/fail-safe-architecture.md.

CONTEXT (verified): the recovery ladder exists (app/services/src/recovery_coordinator.c 4 rungs; app/services/src/sticky_escalator.c 8-rung never-give-up ladder) but 3 of 8 escalator rungs are STUBS (resnapshot :256, self_mint_refold :373, rebootstrap :448 — emit an event and advance). The universal stage_rederive_range primitive (fail-safe-architecture.md §0c) and the LCC (Log-Cursor Contiguity) write-rule invariant (§0b, :80-114) are DESIGN-ONLY. TENACITY.md:106-110 doctrine: "a new wedge class earns a new write-time invariant, never a new repair rung."

HARD RULES: consensus parity inviolable (re-derive means re-fold the SAME bodies to the SAME verdicts — never change validity); recovery rungs stay CONTAINED where required (respect check-no-new-repair-rung / check-no-new-borrowed-seed; a NEW rung that re-derives-in-place from bodies is fine, a borrowed-seed install is not); every malloc via zcl_malloc, every error LOG_*, every write AR lifecycle; files < 800 lines; the LCC rules must NOT weaken any existing consensus guard; NEVER touch a live datadir / mint producer / stop zclassicd; copy-prove before live, gate on H* CLIMB. If your worktree cannot link: run make worktree-init first. OWN isolated worktree; commit to THIS branch only. Self-gate: make build-only + focused test + make lint. Final message = structured data; report the branch. AVOID editing chainstate.c, block_index_loader.c, msg_headers.c, tip_finalize_stage.c, sync_rate_below_floor.c, reducer_drive_watchdog.c, supervisor.c (sibling spine workflows own those).`

phase('SelfHeal')
const lanes = await parallel([
  () => agent(`${COMMON}

LANE stage_rederive_range primitive (the universal re-derive; consensus-critical correctness). Build stage_rederive_range(from_height, to_height) — one general entry that re-derives any range of the 8 stage logs by RE-FOLDING the PoW-verified on-disk bodies to identical verdicts, replacing the ad-hoc detector/refill zoo (fail-safe-architecture.md §0c). Its nearest real analog is reducer_frontier_replay_stale_script_tx / stage_reducer_frontier_reconcile_light — study those and generalize. It must: rewind the affected stage cursors to from_height (respecting LCC — never below the deepest downstream consumer), delete the stale suffix rows, and let the reducer re-fold to_height, producing byte-identical consensus verdicts. Idempotent + crash-safe (a crash mid-rederive self-heals on next fold). Expose it as a clean callable so the recovery ladder rungs use ONE primitive. Focused test: seed a stage log with stale/hashless/rowless rows in a range, call stage_rederive_range, assert the rows are re-derived to correct verdicts + the cursor is contiguous + a downstream consumer advances. Files: a NEW app/jobs/src/stage_rederive_range.c/.h + its test. Do NOT edit the stages themselves; drive them via existing drain/replay seams.`,
    { isolation:'worktree', model:'opus', label:'P2:rederive-range', phase:'SelfHeal', schema:LANE_SCHEMA }),

  () => agent(`${COMMON}

LANE complete the recovery ladder (never give up, always self-derived). The sticky_escalator has 3 STUB rungs (app/services/src/sticky_escalator.c:256 resnapshot, :373 self_mint_refold, :448 rebootstrap) that emit an event and advance without acting. Make the ladder actually remedy in-process, per fail-safe-architecture.md's target collapse (rederive -> restart-with-refold-armed -> page): (1) implement resnapshot as an in-process re-derive from the nearest self-verified rewind base (call the seal-ring rewind API a sibling lane exposes, or stage_rederive_range from the nearest anchor — detect at runtime, fall back cleanly if not present); (2) implement self_mint_refold as arming a from-anchor re-fold of the real bodies; (3) either implement rebootstrap (fresh peer/header re-bootstrap) or collapse it into widen_peers + refold_from_anchor and document the removal. TERMINAL rung must ALWAYS re-derive from folded bodies, NEVER install a borrowed value. Preserve the never-give-up cycling + non-latching operator page on exhaustion. Keep every consensus guard. Focused test: each newly-real rung, given its trigger, performs a witnessed remedy (or names a blocker if its precondition — e.g. a reachable anchor — is absent), and the ladder still terminates/cycles. Files: sticky_escalator.c (+ recovery_coordinator.c ONLY if a rung-class hook is needed).`,
    { isolation:'worktree', model:'opus', label:'P2:complete-ladder', phase:'SelfHeal', schema:LANE_SCHEMA }),

  () => agent(`${COMMON}

LANE LCC write-rules — make a wedge UNWRITABLE (Pillar 4; prevention over cure). Implement the Log-Cursor Contiguity invariant (fail-safe-architecture.md:80-114) at the single cursor-write chokepoint: RAISE/DELETE write rules so that a stage cursor advancing over a ROWLESS hole, or a cursor set ABOVE the durably-applied frontier, or a force-raise past a gap, is IMPOSSIBLE TO PERSIST — the write is refused at the chokepoint with a named error, not repaired later. Find the single place stage cursors + log rows are committed (the progress_store / stage-log write path, e.g. the stage_batch / progress_store cursor-write helper) and add the contiguity assertions there (every cursor advance must have a co-committed contiguous log row; a DELETE must clamp the cursor). This is the doctrine "a new wedge earns a write-time invariant, never a new repair rung." Do NOT weaken any existing consensus guard; the rule only forbids INCOHERENT writes. Focused test: an attempted rowless-hole cursor advance is REFUSED at the write path; a legitimate contiguous advance is accepted; a DELETE clamps the cursor. Files: the cursor-write chokepoint file (identify it — likely lib/storage/src/progress_store*.c or an app/jobs stage-batch helper) + its test. AVOID the stage files owned by siblings.`,
    { isolation:'worktree', model:'opus', label:'P4:lcc-write-rules', phase:'SelfHeal', schema:LANE_SCHEMA }),

  () => agent(`${COMMON}

LANE self-healing regression tests (prove the ladder + primitive + LCC hold). Add focused tests that exercise the self-healing contracts as a suite (test-only; no production code unless a real bug is found): (1) stage_rederive_range re-derives a range to byte-identical verdicts and is idempotent+crash-safe (call it twice, kill-simulate mid-way); (2) the recovery ladder, driven by a synthetic persistent stall, walks its rungs, performs witnessed self-derived remedies, and CYCLES + pages non-latching on exhaustion without ever giving up; (3) the LCC write path REFUSES an incoherent cursor advance. Where a sibling lane's API (seal rewind, stage_rederive_range, LCC assertions) is not yet on your branch, structure the test to detect+skip so it becomes the integration regression gate once merged. Wire into test_parallel.c + agent_impact_rules.def + DOC-COUNTS. Files: lib/test/src/test_always_sync_selfheal.c + registration. No production .c changes.`,
    { isolation:'worktree', model:'sonnet', label:'tests:self-healing', phase:'SelfHeal', schema:LANE_SCHEMA }),
])
return { lanes: lanes.filter(Boolean) }
